{
  "name": "rozmowa-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build && cp -r web/dist ./dist",
    "check:repo": "bash scripts/checks.sh",
    "prepare": "is-ci || husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "is-ci": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
